version: "3.8"

services:
  adsa:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    volumes:
      - ./outputs:/app/outputs
      - ./sample_data:/app/sample_data
    restart: unless-stopped
